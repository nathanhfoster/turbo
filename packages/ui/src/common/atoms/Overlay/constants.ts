import type { BlurType } from "./types";

export const BLUR_CLASSES: Record<BlurType, string> = {
	none: "",
	sm: "backdrop-blur-sm",
	md: "backdrop-blur-md",
	lg: "backdrop-blur-lg",
	xl: "backdrop-blur-xl",
};

