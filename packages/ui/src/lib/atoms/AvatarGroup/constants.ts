import type { AvatarGroupDirection } from './types';

export const AVATAR_GROUP_STACKED_STYLES: Record<AvatarGroupDirection, string> =
  {
    default: '-space-x-4',
    rtl: 'space-x-reverse -space-x-4',
  };
